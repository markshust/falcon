---
name: Dropdown
menu: Components
---

import { Playground } from 'docz';
import { Value } from 'react-powerplug';
import { Box, Backdrop, Portal, Dropdown, DropdownLabel, DropdownMenu, DropdownMenuItem } from '../';

# Dropdown
### Basic usage

<Playground>
  <Box height={300}>
    <Value initial="acending">
       {({ value, set }) => (
          <Dropdown onChange={set}>
            <DropdownLabel>
                Sort by: {value}
            </DropdownLabel>
            <DropdownMenu>
              <DropdownMenuItem value="ascending">
                  Ascending
              </DropdownMenuItem>
              <DropdownMenuItem value="descending">
                  Descending
              </DropdownMenuItem>
            </DropdownMenu>
          </Dropdown>
        )}
    </Value>
  </Box>
</Playground>
